# Copy to .env and fill in (contracts/.env is git-ignored — never commit real keys)

# RPC endpoint for Ethereum Sepolia (e.g. Alchemy/Infura)
SEPOLIA_RPC_URL=""

# Deployer / operator key — TESTNET ONLY
LENDER_PRIVATE_KEY=""
BORROWER_PRIVATE_KEY=""

# Deployed venue addresses (output by script/DeployRepo.s.sol)
MOCK_BUIDL_ADDRESS=""
MOCK_USDC_ADDRESS=""
REPO_VAULT_ADDRESS=""
